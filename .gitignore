diagram-positions.json
diagram-state.json.lock
diagram-state.json.tmp
artifacts/
.DS_Store

# Generated by tests/parity_deno.js (renderer-side measurements; machine-specific)
tests/parity-deno.json

# Local Claude Code settings + scratch captures
.claude/

# Throwaway worktree from `just upstream-check` — removed on exit, but a killed
# run can leave it behind
.upstream-check-worktree/
